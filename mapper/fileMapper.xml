<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="file">
    <insert id="insertFile">
        insert into cmmu_db.file (
            file_name,
            file_extension,
            file_path,
            input_id
        )value(
            #{fileName},
            #{fileExtension},
            #{filePath},
            #{inputId}
        )
    </insert>
</mapper>